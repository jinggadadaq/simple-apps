services:
  simple-app:
    container_name: ct-simple-app-prod
    build: ./app
    image: jinggaadisaputra/img-simpleapps:v1-stable
    ports:
    - "10000:3000"
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M